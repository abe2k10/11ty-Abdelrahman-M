---
title: Everyday Carry
layout: layout
---

<h2>My Everyday Essentials</h2>
<p class="intro">
  These are three things I never leave home without:
  {% highlight "Wallet" %}, {% highlight "Keys" %}, and {% highlight "Hat" %}.
</p>

<div class="grid">
  {%- for item in collections.carry -%}
  <div class="card">
    <a href="{{ item.url }}">
      <img src="{{ item.data.image }}" alt="{{ item.data.title }}">
      <h3>{{ item.data.title }}</h3>
      <p>{{ item.data.short }}</p>
    </a>
  </div>
  {%- endfor -%}
</div>
